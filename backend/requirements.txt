fastapi==0.137.2
uvicorn[standard]==0.48.0
PyJWT[crypto]==2.13.0
# Bumped from 1.24.7 (fix batch #3, ADR-023): CVE-2026-3029 (path traversal /
# arbitrary file write, fixed in 1.26.7) plus two years of upstream MuPDF
# parser fixes — this library parses UNTRUSTED user PDFs, so it stays current.
PyMuPDF==1.26.7
httpx==0.27.0
python-multipart
langgraph==1.2.6
groq
# Sprint 4.6a report generation (D17, ADR-022): builds the downloadable .docx
# deliverable — the single report export format (the fix-batch-#3 fpdf2 PDF was
# removed 2026-07-14: it didn't download reliably, and an editable .docx is what
# users actually want over a locked-in-place PDF). Small, pure-Python (lxml is
# its only heavy wheel, a few MB), imported lazily by the download endpoint
# only. Verified on PyPI 2026-07-13.
python-docx==1.2.0
# Sprint 4.6b: renders validated chart SPECS into PNG figures for the .docx
# export (Agg backend, imported lazily at download time only —
# the ~100MB+ matplotlib/numpy footprint must never load at boot on the
# 512MB dyno; live memory behavior is a GATE-30 verification item).
# Unpinned like groq: pip resolves the newest wheel for the dyno's Python.
matplotlib
# Latest SDK (v4, OTel-based), per Langfuse's official skill guidance ("always
# use latest unless there's a good reason not to"). A prior version of this file
# pinned v2 to dodge the OTel dependency tree; a real memory measurement showed
# the marginal RAM cost over v2 is ~4MB (both already pull in pydantic/httpx),
# negligible next to langgraph's own ~100MB. See docs/ADR-016.md (revised).
langfuse==4.13.1
# Auto-instruments every AsyncGroq client process-wide (model name + token
# usage on every Groq call, nested under whatever Langfuse span is active) with
# no changes needed to orchestrator.py/synthesizer.py/critic.py. Must be
# .instrument()-ed AFTER the Langfuse client is constructed (observability.py).
openinference-instrumentation-groq==0.1.16
